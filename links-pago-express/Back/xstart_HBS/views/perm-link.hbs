<!DOCTYPE html>
<html lang="en">
{{> header }}

<body>
    <div>
        <h1 class='text-center p-3'>
            {{ titulo }}
        </h1>

        {{#if isHasPaylinks}}
        <h4 class='text-center'>
            Selecciona una opcion para proceder a pagar
        </h4>

        <div class="d-flex flex-column justify-content-center">
            {{#each paylinks}}
            <div class="d-flex justify-content-center p-3">
                <a href="{{SOPAYS_PERMALINK}}" target="_self" />
                <img src="/xstar/assets/img/pay-{{ID_PAYS}}.png" alt=" link de pago" />
                </a>
            </div>
            {{/each}}
        </div>
        {{else}}
        <h4 class='text-center'>
            Contacte a  para verificar el estado de su compra
        </h4>
        {{/if}}
    </div>
</body>

</html>